/**
 * Supabase 키 설정 (이 파일을 복사해서 사용)
 *
 * 1. 이 파일을 supabase-config.js 로 복사
 * 2. Supabase 대시보드 → Project Settings → API 에서 URL / anon key 붙여넣기
 * 3. supabase/schema.sql 을 SQL Editor에서 실행
 *
 * supabase-config.js 는 .gitignore 처리되어 GitHub에 올라가지 않습니다.
 */
window.STREAMCS_SUPABASE = {
  url: "https://YOUR-PROJECT.supabase.co",
  anonKey: "YOUR-ANON-PUBLIC-KEY",
};
